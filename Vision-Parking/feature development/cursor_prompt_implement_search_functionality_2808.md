### Feature development of Implementing Search Functionality in Home &ParkingLotOptions screen

![Home Screen Design 1](../../assets/designs/HomeScreen.png)
![Parking Lot Options Design 1](../../assets/designs/ParkingLotOptions.png)

Implement the following changes in the Home Screen(with Map) of my parking app:

-Google Places Autocomplete Search Bar
 Add a Google Places Autocomplete Fragment at the top of the Home screen layout (above the map fragment).

-As the user types, show location suggestions using the Places API.

-When a user selects a location, retrieve its LatLng and move the Google Map 
 camera to that location (instead of the default GPS current location).

-Parking Lot Markers
 Plot all nearby parking lots on the map using colored markers (red, green, yellow pins).

-Each marker should display the parking fee as a text label above the pin (as  shown in the attached image input). 